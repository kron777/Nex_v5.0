# Theory X — Stage 1: Sense

**Phase 2. Raw stream coupling.**

All 23 feed adapters live. Input arrives with timestamps and provenance
before any interpretation. Writes to `sense.db` via
`substrate.Writer` — never direct.

Empty in Phase 1.
