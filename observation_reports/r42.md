# NEX5 — Round 42: fresh spot check

**Date:** 2026-08-09 · **Nothing shipped to the fire path.** No restart.

---

## A. The redraw

### A.1 Set 2 — drawn and frozen

**OBSERVED:**

| | |
|---|---|
| rows | **20** |
| bands | decision **14**, high **4**, low **2** |
| drawn from | the **100** fires not in set 1 (pool: 66 decision / 16 high / 18 low) |
| overlap with set 1 | **0** |
| seed | fresh (`SPOT_SEED_2 = 42`), frozen |
| answered | **0** — `labeller` entirely NULL |
| set 1 | **untouched**, still 20/20 unanswered |

Set 1 is left in place rather than deleted, so R41's account of the leak stays checkable against the actual rows.

### A.2 Nothing about set 2 has been disclosed

Per the blind-check protocol added in R41 §A.5, and applied to this report as much as to the tooling:

- No label counts, ratios, or distribution — **I did not query my own labels for set 2**, and the verification above deliberately omits that column.
- No indication of which items are contested or which band any particular fire sits in.
- No guidance on what to watch for.
- The draw command prints band counts and the run command, nothing else.

The band split (14/4/2) is specified by the brief itself and carries no information about my labels, so stating it is not a disclosure.

### A.3 The κ fix is in the tool, not just the rule

R41 §A.4 was accepted, so the reporting path now computes it rather than leaving it as a written intention. On completion the tool prints **Cohen's κ as primary**, positive-class agreement, and raw agreement marked *context only*.

Verified against R41 §A.4's own worked cases (2 striking each, n=20) — **REASONED construction, OBSERVED output:**

| case | κ | raw | positive-class |
|---|---|---|---|
| overlap 2 of 2 | **+1.000** | 100% | 2/2 |
| overlap 1 of 2 | **+0.444** | 90% | 1/3 |
| overlap 0 of 2 | **−0.111** | 80% | 0/4 |

Raw agreement calls all three *"≥80%, carries over"*. κ separates them. That is the defect R41 identified, now closed in code.

Bands for reading the result, replacing R40 §E's raw-agreement bands:

- **κ ≥ 0.60** — substantial; R40's conclusions carry over.
- **κ 0.20–0.59** — fair/moderate; recompute R40 §B.1/§B.3 against your labels.
- **κ < 0.20** — the analysis is about my taste, not yours. Discard R40 §B.1/§B.3. **§B.2 survives regardless**, because it needs no labels at all.

### A.4 To run it

```
! python3 scripts/label_genius.py --spot
```

`s` = striking, `o` = ordinary, `q` = quit (progress saves per keystroke). It picks up set 2 automatically. Re-run after finishing to print the result.

---

## B. Nothing else

**There is genuinely nothing else safe to do, and I am stopping.**

C2's EXPLAIN read is due after **2026-08-10T16:36:43Z** — its endpoints are EXPLAIN engagement-failure and EXPLAIN on-subject fidelity. The three best-evidenced items on the queue (the F2 sign-flip, retiring F2's rolling reference window, and the quiescent raw-dump rate at `generator.py:800`) all change fire content and all wait on that read. The error ring remains staged-inactive by design.

Confirmed unchanged since R41 — **OBSERVED:** no commits against `generator.py` or `crystallizer.py`; the running process still predates the `errors.py` edit; set 1 still unanswered.

---

## C. Marking

**OBSERVED:** set 2's row count, band split, zero overlap with set 1, zero answers; the remaining pool sizes; set 1 untouched; the κ output table.

**REASONED:** the κ band thresholds (0.60 / 0.20 are the conventional Landis–Koch cut-points, not fitted to anything here); that nothing else is safe to run before the C2 read.

**NOT DISCLOSED, deliberately:** anything about my labels for set 2.

---

**Set 2 drawn from the 100 fires outside set 1 — 14/4/2, fresh seed, frozen, zero overlap, nothing disclosed. Cohen's κ now computed by the tool rather than promised in a rule. Nothing else is safe until C2's read tomorrow, so nothing else was done.**
