# NEX5 — Round 66: self-referencing excess, designed — B=200, θ=22%, blocked on one check

# **VERDICT: CONDITIONAL PASS. Recommend B=200, θ=22% — but condition (c) cannot be evaluated on today's corpus, and shipping is blocked until it can be (~2026-08-17).**
# **(a) FALSE-POSITIVE + SENSITIVITY — PASS.** At B=200: clean-corpus FPR **0.00%** over 253 windows, and Gatwick fires at **30.5%** against θ=22%, inside the required 20% margin (0.8 × 30.5% = 24.4%).
# **(b) BLIND SPOT — PASS, and here is the headline cost: at B=200/θ=22%, a convergence ramping to 34% DF over more than 2.4 days is invisible.** T_max = **193 beliefs**, against the pre-registered bar of 2× the Gatwick ramp = 146. Gatwick took **73 beliefs (1.28 days)**, so it is caught with **1.9× margin** — this detects genuinely slower events than the one it was validated on, which is what bar (b) existed to test.
# **(c) THRESHOLD STABILITY — UNDETERMINED at B=200, and FAILS at B=100 (6 pp against a 4 pp bar).** The disjoint-half test needs ~700 clean documents; there are **502**. **I am not calling this a pass.**
# **The pre-registered grid over-reached: B=800 is not evaluable (0 windows) and B=400 is under-powered (53 windows, FPR resolution 1.9%).** Reported rather than quietly dropped, and **the anti-tuning clause held — I did not add B=150 or B=300 when 100 and 200 straddled the bars.**
# **B=100 fails two independent conditions.** That the failure clause has teeth on a real candidate is the evidence that B=200's passes mean something.
# **Item 3: `register_exclusion.json` and `register_exclusion_v1_contaminated.json` become entirely obsolete; nothing else reads them.** Re-verified from scratch: **zero importers of `corpus_convergence.py`, zero readers outside it, zero cron or systemd callers.**

**Date:** 2026-08-15 · **Report only. Nothing written, nothing deleted. Execution is a later round.**

---

## A. The Gatwick ramp — the reference the failure bar is defined against — OBSERVED

| | |
|---|---|
| peak DF of `societal` | **34%** at 08-04 13:17 |
| last window at ≤10% of peak | 08-03 06:28 |
| **ramp duration** | **73 beliefs = 1.28 days** |
| pre-registered bar (b) | T_max ≥ **146 beliefs** |

## B. Item 1 — baseline length B

### B.1 Grid feasibility — the pre-registration over-reached — OBSERVED

| B | clean windows | FPR resolution | |
|---|---|---|---|
| 100 | 353 | 0.3% | OK |
| 200 | 253 | 0.4% | OK |
| 400 | 53 | 1.9% | **LOW POWER** — cannot resolve a ≤1% target |
| 800 | 0 | — | **NOT EVALUABLE** |

A baseline needs `B + W` documents before its first window. At 502 clean documents,
B=800 has none. **Only B=100 and B=200 are properly evaluable.** The empirical injection
at B=400 is invalid for the same reason — the ramp starts at index 450 and the corpus
ends at 502.

### B.2 Gatwick must fire — PASS at every evaluable B — OBSERVED

| B | θ | peak excess in-window | driver | 0.8 × peak | margin |
|---|---|---|---|---|---|
| 100 | 24% | 32.0% | `societal` | 25.6% | PASS |
| **200** | **22%** | **30.5%** | **`societal`** | **24.4%** | **PASS** |
| 400 | 20% | 32.2% | `societal` | 25.8% | PASS |

### B.3 The blind spot — OBSERVED + analytic

The analytic boundary, derived and then validated against injection:

    excess_peak ≈ p·(W + B) / (2T)      ⇒   T_max = p·(W + B) / (2θ)

Injection into the clean corpus (peak p=0.34, matching Gatwick; **p not re-tuned**):

| B | θ | T=50 | T=100 | T=200 | T=400 | **T_max** |
|---|---|---|---|---|---|---|
| 100 | 24% | 30% fires | 25% fires | 15% no | 9% no | **106 beliefs (1.3 d)** |
| **200** | **22%** | 32% fires | 30% fires | 22% no | 12% no | **193 beliefs (2.4 d)** |
| 400 | 20% | *corpus-limited, invalid* | | | | 382 beliefs (4.7 d) |

Formula agreement where the corpus supports the test is close — B=100/T=100 predicts
25.5% against 25% observed; B=200/T=200 predicts 21.3% against 22%. **The analytic
T_max is trustworthy in the evaluable range.**

| B | T_max | bar 146 | |
|---|---|---|---|
| 100 | 106 | | **FAIL** |
| **200** | **193** | | **PASS** |
| 400 | 382 | | PASS (low-power θ) |

### B.4 Selection

**Largest B with an adequately-measured FPR ≤1% ⇒ B = 200.** B=400's FPR of 0.00% rests
on 53 windows and cannot resolve the target; B=800 is unmeasurable. **B=200.**

**The blind-spot cost, stated as required: at B=200, a convergence that ramps to 34% DF
over more than 2.4 days is invisible.** A slow-burn convergence over a week would be
missed entirely. That is the price of the design and it should be written into §5
alongside the threshold.

## C. Item 2 — threshold calibration

Calibrated on the clean post-R29 corpus (no convergence in it). **The Gatwick window is
never used to set θ** — that would be fitting on the event.

| B | p50 | p90 | p99 | max | **θ (smallest even % with FPR ≤1%)** | achieved FPR |
|---|---|---|---|---|---|---|
| 100 | 15% | 19% | 23% | 25% | 24% | 0.57% |
| **200** | **14%** | **18%** | **21%** | **22%** | **22%** | **0.00%** |
| 400 | 14% | 17% | 18% | 19% | 20% | 0.00% (low power) |

### C.1 Stability — the blocking check — OBSERVED

| B | half A | half B | \|diff\| | bar 4 pp |
|---|---|---|---|---|
| **100** | 26% | 20% | **6 pp** | **FAIL** |
| 200 | — | — | — | **UNDETERMINED** — each half (251 docs) supports ~1 window at B=250 |
| 400 | — | — | — | UNDETERMINED |

**B=100 fails outright**, and the mechanism is clear — a shorter baseline is a noisier
baseline, so θ becomes a property of the sample. **B=200's distribution is visibly
tighter (p99 21%, max 22% versus 23%/25%), which suggests it would be more stable — but
suggestion is not measurement, and I am not recording it as a pass.**

**This resolves soon, and per R63's own lesson the deferral is a rate claim, so here is
the rate.** The disjoint-half test at B=200 needs ~700 clean documents.

| | |
|---|---|
| clean documents now | **502** |
| measured accrual (08-09→08-14, 6 full days) | **80.3/day** |
| shortfall | 198 documents |
| **check becomes runnable** | **≈ 2026-08-17** |

### C.2 Rule-reconstruction check — required by R61/R64 — OBSERVED

**The ≤1% rule applied to maxDF\*'s own current readings emits 44%, not its shipped
25%.**

This is **not** a defect in the rule — it is an independent measurement of how stale
maxDF\*'s threshold is. 25% was calibrated on a pre-R29 corpus; on current data you
would need **44%** to hold a 1% false-positive rate. **A 19-point gap, corroborating
R65's 10.8% precision from a completely different direction.**

Distinct from R61's finding, which was that R31's *stated rule* could not reproduce
R31's *own* choice from R31's own data. That was a defect in a rule's form. This is a
regime shift. Both are reported; they are not the same problem.

## D. Item 3 — fate of the old path — RE-VERIFIED, not cited

| | |
|---|---|
| importers of `corpus_convergence` (any form) | **0** |
| readers of `register_exclusion.json` outside `corpus_convergence.py` | **0** |
| cron entries / systemd user timers | **0** |
| other mentions in the tree | 2 **comments** in `subject_fidelity.py` |

### What the replacement makes obsolete

| artefact | disposition |
|---|---|
| `register_exclusion.json` | **entirely obsolete** — its only reader is `load_register_exclusion()` |
| `register_exclusion_v1_contaminated.json` | **entirely obsolete** — already superseded |
| `corpus_convergence.py`: `_REGISTER_PATH`, `load_register_exclusion()`, `max_df_star(exclusion=…)` | **dead** under the new measure |
| `corpus_convergence.py`: `recent_crystallized()`, `WINDOW`, CLI scaffolding | **reusable** — the new measure needs the same window plumbing |
| `GENIUS_FIDELITY_BASELINE.md` §5 | **rewrite, not delete** — the failure mode it documents is real and R65 confirmed maxDF\* detects it |

### Recommended sequence

| phase | action | exit criterion |
|---|---|---|
| **0 — now → ~08-17** | **wait.** Run the (c) disjoint-half stability check at ~700 clean docs | θ halves agree within 4 pp. **If they do not, the design fails on threshold instability and this recommendation is withdrawn.** |
| 1 | implement excess at **B=200, θ=22%** alongside maxDF\*; mark maxDF\* deprecated but callable | both readings recorded for a **14-day** window |
| 2 | delete the two JSON files and the list dependency; rewrite §5 | zero unexplained excess fires during the window |

**Deletion is a later round regardless. This round deletes nothing.**

---

## E. Exact recommended values

| parameter | value | basis |
|---|---|---|
| window W | **50** | held fixed; maxDF\*'s defined window |
| **baseline B** | **200 beliefs** | largest B with adequately-measured FPR ≤1% |
| **threshold θ** | **22%** | smallest even % with clean FPR ≤1% (achieved 0.00%) |
| blind spot | **ramps slower than 2.4 days are invisible** | T_max = 193 beliefs |
| Gatwick margin | 30.5% vs 22% | 1.4× threshold, 1.9× ramp-duration margin |
| **status** | **BLOCKED on (c) until ~2026-08-17** | 502 / ~700 clean docs at 80.3/day |

---

## Carry-forward

1. **The failure clause bit a real candidate.** B=100 fails (b) and (c) independently.
   A pre-registration whose bars nothing can fail is decoration; this one excluded the
   shorter baseline on evidence.
2. **The grid was wrong and that is reported, not hidden.** Two of four B values were
   unevaluable on the corpus I had. I did not substitute nearer values to fill the gap —
   a later round may pre-register a finer search **as** a search.
3. **The 44% figure is the round's incidental find.** It quantifies maxDF\*'s threshold
   staleness at 19 points from a direction entirely independent of R65's precision
   measurement, and the two agree.
4. **One check away, and it is a short, verifiable wait — not another 1,000-doc saga.**
   Two and a half days at a measured 80.3/day, on a corpus that grows on its own.
5. **Nothing was written.** `register_exclusion.json`, `GENIUS_FIDELITY_BASELINE.md`,
   `corpus_convergence.py` untouched.
