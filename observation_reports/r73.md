# NEX5 — R73: verification read — A holds, B is not ready, C is not datable yet

**Date:** 2026-08-21 16:10 UTC · **Read-only. Nothing shipped, nothing refit, no scorer, no
relevance retrieval.** The one code change on disk (R72) was already committed before this
round and is untouched here.

---

# VERDICTS FIRST

| | verdict |
|---|---|
| **A — burst-template fix** | **HOLDS.** 0 created in 3.12 days, from 3.4/day. Not over-matching. **But it is R71's fix, not R72's — see A0.** |
| **B — register-refit window** | **NOT READY.** Clean *and* adequate does not exist: the 1,047-doc window is contaminated at its head by the previous convergence; the genuinely clean part is **575** against R50's ~1,000 floor. |
| **C — stability test** | **Still blocked, and not datable today** — the contaminant has not peaked. Earliest honest date **~2026-08-31**, and that is a floor, not a forecast. |

---

# A0. Three premises in the brief are wrong — OBSERVED

**These change what A can be measured against, so they come first.**

| brief says | actual |
|---|---|
| "R72 shipped `677c9b4`" | **`677c9b4` is the pre-registration — one markdown file, 257 lines, no code.** The R72 code is **`4027449`**. |
| "R72 = branch-agnostic entity-recurrence throttle" | **R72 is the opposite of a throttle.** A branch-agnostic throttle was option 1 in R71 §B1 and was **rejected** (reaches ~1/day, not ~0, and needs a text predicate that can over-match). R72 *widens* the entity gate so real problems can open. |
| "R72 … live" | **R72 has never executed.** It binds `_PROMOTABLE_TYPES` and `_entity_counts` at import; pid 2354 started **15:26:00Z**, the edit landed **15:54:54Z**, and there has been no restart since. **Uptime of the change: zero seconds.** |
| "the R70 reaper stays staged-inactive" (§D) | **Already live since 08-18 and already drained.** `signals` holds **77,003** rows, down from 1,394,502. R71 recorded this. |

**Consequence for A: the burst-template suppression the brief attributes to R72 is R71's
type removal (`d3c6e03`, live 2026-08-18T13:05:11Z).** That is what the numbers below
measure. **A cannot be a verification of R72, because R72 has not run.** Reporting it as one
would credit an inert file.

---

# A. Did the burst-template fix work? — **HOLDS** — OBSERVED

## A1 — creation rate

| since R71 went live (3.12 days) | |
|---|---|
| burst-template problems created | **0** |
| pre-fix rate | **3.4/day**, 34.5% of all problems |
| `t6_promotion_burst` signals that arrived anyway | **497** |
| …promoted | **0 (0.0%)** |

**497 signals arrived, all above `MIN_CONFIDENCE`, and not one became a problem.** The
suppression is at the type, so the promotion query cannot return them at all.

## A2 — falsifier: **not triggered**

Falsifier was *"still created at the old rate."* Observed rate is **0.0/day against 3.4/day**.
The 161 pre-existing burst problems have aged out: **#468 is the only one left non-closed**,
and it was created 08-18 05:30, *before* the fix.

## A3 — tripwire: legitimate creation — **the number is 0/day, and it was 0/day before**

**This is the one number in A that is not a clean pass, and it must not be read as one.**

| | |
|---|---|
| non-template problems created since the fix | **0** |
| non-template problems created in the 21 days *before* the fix | **0** |
| last entity-named problem of any kind | **2026-08-12** |

**The tripwire as written cannot fire.** It asks whether legitimate creation *dropped*; it
was already zero six days before the fix went live, so there is nothing for the fix to have
dropped. **This is not the throttle over-matching** — R71 §C5 established the cause: the
recurrence gate counted over the tick's 20-row batch instead of the 6-hour window, so
**0 of 149 reconstructed ticks ever cleared the 3-mention bar.** That is the defect R72
fixes, and R72 has not run.

## A4 — is a shared entity suppressing distinct real problems? — OBSERVED

**No branch-agnostic throttle exists to check.** The mechanism that *does* collapse distinct
problems sharing an entity is `_has_recent_dupe`:

```sql
WHERE state IN ('open','stuck') AND last_touched_at > ?
  AND (title LIKE '%entity%' OR description LIKE '%entity%')
```

**It matches the entity as a substring of the title *or the description*, for 7 days.** So
two genuinely distinct Bitcoin problems do collapse into one, and an unrelated problem that
merely *mentions* Bitcoin in its description blocks a real Bitcoin problem from opening.
R72's dry run showed this directly: **`considered: 18, opened: 1, skipped_dupe: 17`.**

**By design, and not this round's to change** — but it is the real answer to the question A4
is reaching for, and it will become the binding constraint the moment R72 starts opening
problems. **Flagged for whichever round takes it.**

## A5 — verdict

**FIX HOLDS.** Not over-matching, not failed. **With the qualifier that the queue it
protects is empty for an unrelated reason, which R72 addresses and which is still inert.**

---

# B. Is the register-refit window clean and adequate? — **NOT YET** — OBSERVED

## B1 — maxDF\* across the window, and what is driving it

**The alarm is in breach right now and has been almost continuously** — `WINDOW=50`,
`THRESHOLD=0.25`:

```
LIVE maxDF* = 32.0%  on 'item'   BREACH
   top: item 32.0% | highlights 24.0% | advancements 24.0% | societal 20.0%
```

| day | maxDF\* | driver | | with `item` also excluded | driver |
|---|---|---|---|---|---|
| 08-09 | 23.4% | discusses | | 23.4% | discusses |
| 08-11 | **30.0%** | **item** | | 26.7% | engagement |
| 08-12 | **36.6%** | **item** | | 23.7% | aligns |
| 08-16 | **40.0%** | **item** | | 25.9% | aligns |
| 08-18 | **37.0%** | **item** | | 23.9% | systems |
| 08-20 | **27.2%** | **item** | | 24.7% | societal |
| 08-21 | **37.8%** | **item** | | 24.3% | highlights |

**Breach on 11 of 13 days, and the driver is register, not topic.** `item` is the fountain's
stock framing — *"The item discusses…"*, *"This item states…"*, *"This item discusses…"* —
and it is **not among the 38 terms in `register_exclusion.json`**. Nor are `highlights`,
`discusses`, `aligns`, or `underscores`.

**This is exactly R50's diagnosis, now considerably worse.** R50 measured 18–24% on
`aligns`/`discusses` against a 25% threshold and called the list's defect *staleness, not
contamination*. **`item` now reads 30–40% on its own.** With `item` excluded the drivers are
still all register vocabulary, still breaching on 5 of 13 days. **The alarm currently
measures the fountain's prose habits, and has no headroom left.**

## B2 — the two convergences: **not under 5%, and not decaying** — OBSERVED

**Consecutive days with both under 5%: 0. The rule needs 3.**

| day | `societal` | `advancements` |
|---|---|---|
| 08-19 | 8.9% | 21.5% |
| 08-20 | **24.7%** | 22.2% |
| 08-21 | 18.9% | **24.3%** |

Per 6-hour bucket the most recent reading is **28.6% / 42.9%** — `advancements` is at its
**highest yet**. `societal` appears to have peaked 08-20; **`advancements` has not peaked at
all.**

## B3 — why no clean adequate window exists — OBSERVED

The refit needs **~1,000 clean post-R29 documents** (R50's stability floor; R29 on
2026-08-03 tripled document length from 9.6 to 27.7 mean content tokens, so DF is not
comparable across it).

| candidate | n | clean? |
|---|---|---|
| 08-03 → 08-16 | **1,047** | **NO** — `societal` runs **8.6–24.2%** across 08-03…08-08 |
| **08-09 → 08-16** | **575** | **YES** — both contaminants under 5% every day |
| 08-16 → now | 466 | **NO** — the live convergence |

**The 1,047-document window is the tail of the previous convergence.** This is also, finally,
the explanation for the otherwise-unexplained `2026-08-09` window start used since R67:
**08-09 is simply the first day `societal` fell under 5%** (3.9%) after the Gatwick episode.

**So: adequate but dirty, or clean but 575 against a 1,000 floor. Shortfall 425 documents.**

## B4 — verdict and projected date — REASONED from OBSERVED

**NOT READY.** Still contaminating: `advancements` (from 08-16, not yet peaked) and
`societal` (from 08-19, peaked 08-20).

**The one empirical basis for projecting is the previous episode**, which is the same token
with a near-identical peak:

| | previous (`societal`) | current (`societal`) |
|---|---|---|
| first sustained breach | 08-03 (13.0%) | 08-19 (8.9%) |
| peak | **08-04 — 24.2%** | **08-20 — 24.7%** |
| under 5% | 08-09 | **→ ~08-25 if it repeats** |
| **peak → clear** | **5 days** | |

**Projection, stated with its weakness: n = 1.**

| | earliest |
|---|---|
| `societal` clears | **~08-25** (peak + 5 days, one prior instance) |
| `advancements` clears | **unknown — it has not peaked.** ≥ 08-26 on the same shape, later if it keeps climbing |
| **contamination clears (X)** | **~08-26, floor not forecast** |
| refit ready, **splicing** 575 + 425 new clean docs @ 84.5/day | **X + 5.0 d ≈ 2026-08-31** |
| refit ready, **contiguous** fresh 1,000 | X + 11.8 d ≈ 2026-09-07 |

**The splice needs a ruling and I am not making it.** R50's floor was stated for a
contiguous window (its own accepted case was 06-01→07-28, n=1627). For a **register refit**
— a bag-of-documents vocabulary fit — a 10-day hole is arguably harmless. **It is worth a
decision because it is the difference between ~08-31 and ~09-07.**

---

# C. Stability test status — **blocked, earliest honest date ~08-31**

## C1 — still blocked, same cause — OBSERVED

Unchanged from R72 §1: both convergences sit in **half B** of any split of the current
corpus (`societal` half A 1.0% vs half B 7.3%; `advancements` 2.5% vs 16.0%). maxDF\* fires
on real convergences, FPR calibration scores them as false positives, **θ_B is pushed up and
θ_A is not — a spurious FAIL routing into Branch B**, which then commissions a 1,200-document
B=400 test on what is a news cycle.

The restarts remain **not** the cause: every restart boundary measured clean against a
position-matched null (z between −0.27 and −0.08).

## C2 — earliest honest re-date — REASONED

The test needs **800 single-regime documents** (R67 §1: 2 × (200 + 50 + 150)).

| | earliest |
|---|---|
| **splice** 575 clean + 225 new @ 84.5/day | X + 2.7 d ≈ **2026-08-29** |
| **contiguous** fresh 800 | X + 9.5 d ≈ **2026-09-05** |

**For this test I do recommend contiguous, unlike B.** R67 §2 specifies *"two disjoint,
**time-ordered** halves"* and the quantity being measured **is** temporal stability of θ. A
corpus with a ten-day hole in it puts that hole at or near the split point, and whatever
|θ_A − θ_B| then reports, it is not what the test was designed to ask. **The register refit
can defensibly splice; this one should not.**

**So the earliest honest date is ~2026-09-05 done properly, or ~2026-08-29 if a splice is
ruled acceptable — and both are floors resting on an `advancements` peak that has not
happened yet.** Not forced, per §D.

---

# D. Ship status

**Nothing shipped. Read-only round.** No refit, no scorer, no relevance retrieval, no
fire-path change.

**Correction to §D's premise:** the R70 reaper is **not** staged-inactive awaiting a restart.
It went live 2026-08-18T13:05:11Z and has already drained the table to **77,003** rows from
1,394,502. There is nothing left staged to ride a restart.

**What is genuinely pending a restart is R72** (`4027449`) — the batch-vs-window scope fix,
still inert, with success and revert bars pre-registered at `677c9b4` and widened at
r72.md §2.6.
