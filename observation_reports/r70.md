# NEX5 — R70: one defect shipped (staged), one premise falsified, no restart taken

# **A — NOT SHIPPED. The premise is falsified: there is no gerund to block.**
# The burst title is composed at `signal_to_problem.py:176` as `f"Why is {branch} producing strong beliefs right now?"` — **the only variable is `{branch}`**. "producing strong beliefs right now" is literal template text, never an extracted token. The R27 blocklist (`_VAGUE_ENTITY_WORDS`, 130 words) is consulted **only** for titles matching `Signal: investigate '{entity}'` (gate at lines 303/319). **Adding any gerund to that list is a guaranteed no-op for this template.** The brief required "same mechanism, not a new one" — and only a *different* mechanism can work. **Not substituted, per protocol.**
# **B — SHIPPED, STAGED INACTIVE.** `theory_x/signals/reaper.py`, R27 residue-reaper pattern, 7-day retention, hourly throttle, 50,000-row batches. **Zero-consumer claim CHECKED, and R69's summary was wrong: there are FOUR readers, not zero** — one of them on the fire path.
# **C — RESTART NOT TAKEN, and the Aug 18 baseline shift was NOT incurred.** The authorisation was for *two* fixes on one restart. A is a no-op, so the restart now buys one non-urgent change, hours before the stability test. **That is a changed premise and the call is Jon's, not mine.** One word and I'll restart.
# **FIRE PATH BYTE-IDENTICAL.** Only `signals/loop.py` (+3 lines) and the new `reaper.py`. Neither is in the 50-module fire-path closure (R68).

**Date:** 2026-08-17 · **No restart. No scorer work. No relevance-retrieval ship.**

---

# A. Burst-template leak — PREMISE FALSIFIED, NOT SHIPPED

## A1 — the specified change cannot work — OBSERVED

The brief asks for "the missing gerund(s) added to the same block list R27 used". Traced:

**Where the title is made** — `theory_x/signals/signal_to_problem.py:170-180`:

```python
if signal_type == "t6_promotion_burst":
    branches = payload.get("branches") or []
    branch = branches[0] if branches else "substrate"
    return f"Why is {branch} producing strong beliefs right now?"      # line 176
```

**The only substituted value is `{branch}`** — `emerging_tech`, `systems`, `ai_research`,
`cognition`. The words *producing strong beliefs right now* are a string literal. **No
gerund token is ever passed to a gate.**

**Where the blocklist applies** — same file:

| line | |
|---|---|
| 150 | `if lower in _VAGUE_ENTITY_WORDS: return False` |
| 189 | `return f"Signal: investigate '{entity}'"` |
| 303, 319 | gate condition: `title.startswith("Signal: investigate '")` |

**The blocklist governs one title shape, and the burst template is a different shape
built on a different branch of the same function.** The two never meet.

Confirmed by direct inspection of the list: 130 words, and `producing`, `emerging`,
`pattern`, `strong`, `beliefs` are all absent — but **adding them changes nothing**,
because the list is never consulted for `Why is …` titles. `cf99584`'s own commit
message says so explicitly: *"these bypassed the investigate-gate (wrong title prefix)"*.

**This is why `cf99584` shipped a 24h throttle instead of a blocklist entry in the first
place — that route was already known not to work.**

## A2/A3 — the checks the brief asked for, run anyway

**A3 sibling check (R26 Anchoring/Hunting/Scaling):** 0 non-template problem titles
contain "producing". So the proposed block would not hit legitimate titles — **but it
also would not hit the target.** The check passes vacuously.

**A2 offline prediction: not made.** A prediction of "~0 creations" would be false —
the observable outcome of adding a gerund to `_VAGUE_ENTITY_WORDS` is **no change at
all**, ~3.4/day continuing. Stating a falsifier for a change that cannot act would be
theatre.

## What *would* work — REASONED, and deliberately not shipped

Two mechanisms, both **different** from the one specified, so both out of scope:

1. Remove `t6_promotion_burst` / `pattern_recognition_burst` from `_PROMOTABLE_TYPES`
   (`signal_to_problem.py:34-43`) — stops promotion at source.
2. Widen the `_BURST_TITLES` throttle beyond 24h (line 338).

**Neither is urgent.** R69 measured this as benign churn: 161 of 162 closed, average
lifetime 35.1 h against 171.1 h for everything else, and the live queue held exactly
one. **It wants its own round with its own pre-registration.**

---

# B. Signals reaper — SHIPPED, STAGED INACTIVE

## B1/B2 — the zero-consumer claim, CHECKED — **R69's summary was wrong** — OBSERVED

The brief said not to trust R69's "nothing ever consumes them". Correct instinct:
**four readers exist.**

| reader | window | on fire path? |
|---|---|---|
| `signal_to_problem.py:250` | `detected_at > now − 21600` (**6 h**) | no |
| **`competing_drives.py:174`** | `detected_at > now − 86400` (**24 h**) | **YES** |
| `probes/context_snapshot.py:87` | `detected_at > now − 600` (**10 m**) | no |
| `gui/server.py:3139` | newest ≤100 rows, no time bound | no |

R69's claim was about `actioned_at` — *nothing ever **actions** a silence signal* — which
remains true (0 of 1,316,570). **But "never actioned" is not "never read".**
`competing_drives._signal_breakdown()` counts signals by type over 24 h to compute
`cross_domain` and `new_entity` drive rates. **A fire-path file reads this table.**

**This is exactly the R69→R70 version of the recurring error in this series: a number
used for a claim narrower than what it measured.**

**Retention set at 7 days = 7× the binding 24 h window.** No reader can observe a row the
reaper is eligible to delete.

**`patterns.signal_ids`** — checked separately. Written as a JSON blob
(`signals/loop.py:114`), selected for display at `gui/server.py:3143`, **joined back to
`signals` nowhere.** Reaping orphans no dereferenced reference.

**Additional safety beyond age:** the predicate keeps *any* unconsumed promotable row
regardless of age, so the reaper can only remove rows that are already actioned or of a
type nothing consumes.

## B3 — offline prediction — OBSERVED (read-only dry run)

| | |
|---|---|
| rows now | **1,394,502** |
| **eligible to delete** | **1,318,704 (94.6%)** |
| kept — newer than 7 days | 10,752 |
| kept — unconsumed promotable, any age | 65,046 |
| partition check | 1,318,704 + 10,752 + 65,046 = 1,394,502 ✓ |

Eligible by type: `branch_silence_anomaly` 1,307,419 · `t6_promotion_burst` 6,131 ·
`2_branch` 4,533 · `3_branch` 450 · `4_branch` 171.

**Predicted after full drain: ~10,752 rows, signals table ~2.5 MB (from 329.0 MB),
~326 MB reclaimed** — though `beliefs.db` will not shrink on disk without a `VACUUM`,
which is **not** part of this change.

**Batch size is measured, not guessed.** Timed on a byte copy of the live 763 MB
`beliefs.db` — never on the live file:

| batch | lock |
|---|---|
| 5,000 | 14 ms |
| **50,000** | **124 ms** ← chosen |
| 100,000 | 266 ms |

~2.5 µs/row. At 50,000 the lock is ~124 ms once an hour against a fountain firing every
~2.7 min. **Backlog drains in ~27 passes ≈ 27 hours.** My first constant (5,000) would
have taken 11 days; the measurement corrected it.

## Tests — OBSERVED

`tests/test_signals.py`: **5 failed / 16 passed — identical with and without the change**
(verified by stashing `loop.py` and holding `reaper.py` aside). All five pre-exist
(`no such table: world_predictions`). Full suite 39 failed / 1172 passed, all
pre-existing. **The reaper adds zero new failures.**

---

# C. Ship status

## C2 — fire path byte-identical — OBSERVED

```
$ git status --porcelain -- crystallizer.py generator.py competing_drives.py groove.py
(empty)
```

R70 touches exactly two files:

| file | change |
|---|---|
| `theory_x/signals/reaper.py` | new |
| `theory_x/signals/loop.py` | +3 lines in `_tick()` — throttled call, wrapped, never raises |

**Neither is in the 50-module fire-path closure (R68).**

## C1 — restart NOT taken, and why — REASONED

**The Aug 18 stability-test baseline shift was NOT incurred, because no restart happened.**

Jon's authorisation was explicit: *"Ship both now on a single restart."* One of the two is
a no-op. That is material information that did not exist when the trade was accepted, and
the arithmetic has changed:

- the restart now buys **one** change, not two;
- that change is **not urgent** — 115 days of accumulation, and once started it drains in
  27 hours whenever it starts;
- the clean corpus is at **737**, so the **≥700 path is already met and ≥800 lands within
  hours**. A restart resets in-memory fingerprint state in `groove.py` and `detectors.py`,
  which perturbs crystallization briefly — inside the accrual window, hours from the test.

**Spending the baseline shift on a single deferrable reclaim is a decision for Jon, not
one I should make by proceeding on an authorisation whose premise changed.** The reaper is
staged and inert; it activates on the next process start, whenever that is. **Say the word
and I restart immediately** — or it rides along free on the next restart after the test.

## C3 — post-restart health

**Not applicable — no restart taken.** When one happens, the checks stand as briefed: fire
rate ~22/h, no new traceback signatures, and `reap()` confirmed to have run once (visible
as a drop of 50,000 rows in `signals` within the first hour).

---

## Ledger entry

Recorded in `journal/DEPLOY_LEDGER.tsv` as `code_staged_inactive` — **not**
`code_activated`, and **no** `keepalive_start`. It states plainly that A was dropped on a
falsified premise, that B is inert until the next process start, and that **the Aug 18
baseline shift was not incurred because no restart was performed.**

## Scope

No fire-path change. No restart. No scorer work. No relevance-retrieval ship. A not
shipped and not substituted.
